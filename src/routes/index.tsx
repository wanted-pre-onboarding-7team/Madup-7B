import MediaStatus from './MediaStatus';

import styles from './Routes.module.scss';

const App = () => {
  return (
    <div className={styles.app}>
      <MediaStatus />
    </div>
  );
};

export default App;
